# Core
python-dotenv>=1.0
httpx>=0.27
opencv-python>=4.9
Pillow>=10.0
pyyaml>=6.0
albumentations>=1.4

# Training — CUDA path
torch>=2.2; sys_platform != "darwin"
transformers>=4.40; sys_platform != "darwin"
peft>=0.10; sys_platform != "darwin"
bitsandbytes>=0.43; sys_platform != "darwin"
accelerate>=0.29; sys_platform != "darwin"
datasets>=2.19; sys_platform != "darwin"

# Training — Apple Silicon path
mlx-lm>=0.12; sys_platform == "darwin"

# Notifications
python-telegram-bot>=21.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
